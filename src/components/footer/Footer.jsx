import React from 'react';
import stl from './footer.module.css';

const Footer = () => {
    return(
        <footer className={stl.footerStyle}>
            <p>footer footer</p>
        </footer>
    );
};
export default Footer;